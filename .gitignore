/bin/
/keys/
/store/
*.test
*.out
*.swp
.DS_Store
.idea/
.vscode/

# Hugo docs site (site/) build output. The theme is a Hugo Module, so
# nothing vendored needs committing — only go.mod/go.sum, which pin it.
/site/public/
/site/resources/
/site/.hugo_build.lock

# Review handoffs stay local: they describe findings before the fix ships.
/HANDOFF-*.md
